export const Logo = ({ className = 'text-logo' }) => {
    return (
        <span
            className={`${className} font-primary font-bold select-none bg-grey py-[0.2em] px-[0.4em] rounded-lg text-black`}>
            KEAI
        </span>
    )
}
